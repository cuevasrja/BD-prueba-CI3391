services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: postgres_local
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_NAME:-appdb}
      POSTGRES_USER: ${DB_USER:-app}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-app123}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./sql/extensions.sql:/docker-entrypoint-initdb.d/05-extensions.sql
      - ./sql/schema.sql:/docker-entrypoint-initdb.d/10-schema.sql
      - ./sql/load.sql:/docker-entrypoint-initdb.d/20-load.sql
      - ./sql:/scripts
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-app} -d ${DB_NAME:-appdb}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
